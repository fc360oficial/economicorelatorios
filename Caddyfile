hhk0a8gt2cn.sn.mynetname.net {
    reverse_proxy localhost:3003
}

# Outros sites hospedados no mesmo servidor (Servidor_BI). A config deles fica fora
# deste repositório, porque o deploy (git reset --hard) sobrescreve este arquivo e
# apagava o bloco do CAHU Delivery toda vez.
import C:/cahudelivery/infra/caddy/*.caddy
